# MANIFEST — .

> Keystroke-telemetry

*Auto-generated by pigeon_compiler manifest_builder | 2026-03-16 02:44 UTC*

## How to read this manifest

This file is **auto-generated** by `manifest_builder` and describes every
Python module in this folder. It is the single source of truth for:
- What each file does (Description)
- What each file exports (Exports) and depends on (Deps)
- Whether the file meets the Pigeon size budget (Status)
- Living operator notes that persist across rebuilds (Notes)

**Status icons:** ✅ ≤200 lines | ⚠️ OVER 200–300 | 🟠 WARN 300–500 | 🔴 CRIT >500

**Columns:** Seq = load order · Lines = source line count · Exports = public classes/functions · Deps = intra-project imports


## Files

| Seq | File | Lines | Status | Exports | Deps | Description | Notes |
|-----|------|------:|--------|---------|------|-------------|-------|
| 001 | _test_import_rewriter_patch.py | 107 | ✅ | IMPORT_MAP, STEM_MAP, check() | import_rewriter | verify all import patterns are handled. | import_rewriter |
| 002 | stress_test.py | 322 | 🟠 WARN | SCENARIOS, classify_state(), STATE_COLORS, RESET, DIVIDER +6 | logger, resistance_bridge | operator state recognition via typing telemetry. | logger, resistance_bridge |
| 003 | test_all.py | 221 | ⚠️ OVER | sim_type(), sim_backspace(), test_logger(), test_context_budget(), test_drift_watcher() +2 | logger, context_budget, drift_watcher, resistance_bridge | exercises logger, context budget, drift watcher, resistance bridge. | logger, context_budget, drift_watcher, resistance_bridge |
| 004 | test_public_release.py | 30 | ✅ | REPO_ROOT, main() |  | Focused release-readiness checks for public source distribution. |  |

## Health
- **Files:** 4 | **Compliant:** 2/4 (50%)
- **Total lines:** 680 | **Est. tokens:** ~0 | **Target:** ≤200 per file
- **Action needed:** 2 file(s) over limit

## Module Signatures

Function and class signatures for LLM context (type hints included):

**_test_import_rewriter_patch**
```python
def check(name, line, expected)
```

**stress_test**
```python
def classify_state(wpm, hesitation, deletion_ratio, pause_ratio)
def render_header()
def render_scenario_header(idx, total, label)
def render_stats_panel(stats, state_label)
def render_transcript_line(buffer_text, cursor)
def simulate_scenario(logger, scenario)
def main()
```

**test_all**
```python
def sim_type(logger, text, buf = '', wpm = 80)
def sim_backspace(logger, buf, count = 1)
def test_logger()
def test_context_budget()
def test_drift_watcher()
def test_resistance_bridge(summary_path: str)
def main()
```

**test_public_release**
```python
def main()
```


## Constants & Configuration

| Module | Constant | Value |
|--------|----------|-------|
| _test_import_rewriter_patch | `IMPORT_MAP` | `{'production_auditor.pipeline_seq015_v005_d0314__old_desc_lc_old_intent': 'pr...` |
| _test_import_rewriter_patch | `STEM_MAP` | `_build_stem_map(IMPORT_MAP)` |
| stress_test | `SCENARIOS` | `[{'label': 'FOCUSED — fast confident typing', 'text': 'The streaming layer ha...` |
| stress_test | `STATE_COLORS` | `{'FLOW': '\x1b[92m', 'FOCUSED': '\x1b[32m', 'NEUTRAL': '\x1b[37m', 'HESITANT'...` |
| stress_test | `RESET` | `'\x1b[0m'` |
| stress_test | `DIVIDER` | `'─' * 72` |
| test_public_release | `REPO_ROOT` | `Path(__file__).resolve().parent` |

## Code Markers

*No TODO/FIXME/HACK markers found — codebase is clean.*
