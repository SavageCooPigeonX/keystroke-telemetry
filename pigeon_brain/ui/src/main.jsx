import React from 'react';
import ReactDOM from 'react-dom/client';
import PigeonBrain from './PigeonBrain';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PigeonBrain />
  </React.StrictMode>
);
