# pigeon_brain/flow — context-accumulating dataflow engine
# Packets of task cognition flow through the code graph.
# Each node wakes, contributes intelligence, goes back to sleep.
# The path IS the computation. The output IS accumulated state.
