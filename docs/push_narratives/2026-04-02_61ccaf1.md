# Push Narrative — 61ccaf1

**Intent**: git_plugin_unicode  
**Date**: 2026-04-02 15:33 UTC  
**Files**: 1  

- `pigeon_compiler/git_plugin.py`

---

I was touched because the git plugin’s commit message parsing was silently corrupting non-ASCII characters, turning them into replacement characters or mojibake. The fix was to decode the raw bytes from `git log` using the system’s preferred encoding (locale.getpreferredencoding()) with errors='replace' to ensure a safe fallback. My assumption is that the system locale encoding is a reasonable guess for the encoding of commit metadata, which could break in CI environments or on systems with a misconfigured locale, causing us to still lose information or mis-decode if the git output uses a different encoding like UTF-8 on a Latin-1 locale. Watch for regression where commit messages from repositories with mixed or intentional non-standard encodings (e.g., old Windows CP1252 commits) are displayed incorrectly or cause downstream formatting issues.

I have no cross-file dependencies for this change; I only receive raw byte strings from the `git log` subprocess and now pass decoded strings to the rest of the system. If that contract changes and the subprocess call is altered to output text directly, my decoding logic would become redundant and could double-encode.

The operator’s moderate deletion ratio suggests hesitation around choosing the error-handling strategy ('replace' vs. 'ignore' vs. strict) and whether to attempt charset detection, ultimately opting for the simpler, safer replacement.

REGRESSION WATCHLIST: System locale mismatch causing mis-decoded git history, loss of information from 'replace' error handler, incorrect display of legacy commit encodings.
This push ensures git commit metadata containing Unicode characters is preserved and displayed without corruption.
