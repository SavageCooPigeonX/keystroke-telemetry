# Push Narrative — a65a380

**Intent**: update_75_stale  
**Date**: 2026-04-02 01:56 UTC  
**Files**: 55  

- `_fix_stale_globs.py`
- `_run_abbrev_rename.py`
- `_run_glyph_rename.py`
- `_run_smart_rename.py`
- `_tmp_check_rename.py`
- `_tmp_find_stale.py`
- `client/os_hook.py`
- `client/telemetry_cleanup.py`
- `pigeon_brain/__main__.py`
- `pigeon_brain/context_veins.py`
- `pigeon_compiler/cut_executor/__init__.py`
- `pigeon_compiler/git_plugin.py`
- `pigeon_compiler/rename_engine/__init__.py`
- `pigeon_compiler/rename_engine/heal_seq009/f_he_gf_s002_v001.py`
- `pigeon_compiler/rename_engine/heal_seq009/f_he_ie_s003_v001.py`
- `pigeon_compiler/rename_engine/heal_seq009/f_he_lw_s004_v001.py`
- `pigeon_compiler/rename_engine/heal_seq009/f_he_rt_s001_v001.py`
- `pigeon_compiler/state_extractor/__init__.py`
- `pigeon_compiler/weakness_planner/__init__.py`
- `src/cognitive/drift/u_dbcc_s005_v001.py`

---

**_fix_stale_globs** was created to surgically remove stale glob patterns from configuration files, assuming that glob entries are always on their own lines and can be identified by a specific comment marker; if that marker format changes or globs span multiple lines, this will silently fail, leaving corrupted configs. It receives file lists from `_tmp_find_stale` and expects clean paths—watch for it deleting active globs if the input list is polluted.

**_tmp_find_stale** was added to scan for unused glob patterns by diffing current files against git history, assuming `git ls-files` provides a complete and accurate list of tracked files; if git’s output format changes or untracked files are relevant, it will produce false positives, causing the glob fixer to over-clean. It feeds `_fix_stale_globs`—a break here cascades.

**git_plugin** was modified to expose a new method `get_tracked_files` for the stale-finding process, assuming the plugin’s command execution context remains stable; if the subprocess call fails or the working directory is wrong, the entire stale detection pipeline will return empty and skip cleanup. It gives raw file lists to the tmp scripts.

**pigeon_compiler/rename_engine/heal_seq009/*** files were updated to reference the new stale-cleaning utilities, assuming the utilities exist at import time and return deterministic results; if those modules raise during import, the rename engine’s healing sequences will fail to load, breaking all rename operations. They receive cleanup triggers from the rename context.

**u_dbcc_s005_v001** was touched to integrate stale glob awareness into cognitive drift analysis, assuming the new `_tmp_find_stale` output is a list of strings; if the return type shifts to a dictionary or generator, the drift scoring will crash with attribute errors. It takes glob lists from the fixer module.

**REGRESSION WATCHLIST:** `_tmp_find_stale`'s git parsing fragility, `_fix_stale_globs`'s line-based deletion assumption, `git_plugin`'s new method integration point. This push adds automated cleanup of obsolete glob patterns from configs to reduce tooling noise.
