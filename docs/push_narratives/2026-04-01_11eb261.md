# Push Narrative — 11eb261

**Intent**: confidence_scorer_glyph  
**Date**: 2026-04-01 19:21 UTC  
**Files**: 12  

- `pigeon_compiler/rename_engine/nametag_seq011_v004_d0401__encode_file_description_intent_into_lc_confidence_scorer_glyph.py`
- `pigeon_compiler/rename_engine/planner_seq002_v005_d0401__generate_rename_plan_for_non_lc_confidence_scorer_glyph.py`
- `src/confidence_scorer_seq033_v002_d0401__per_module_copilot_confidence_scorer_lc_confidence_scorer_glyph.py`
- `src/research_lab_seq029_v006_d0401__the_system_studying_the_system_lc_confidence_scorer_glyph.py`
- `src/symbol_dictionary_seq031_v003_d0401__symbol_dictionary_generator_for_pigeon_lc_confidence_scorer_glyph.py`
- `pigeon_compiler/git_plugin.py`
- `pigeon_compiler/rename_engine/nametag_seq011_v003_d0314__encode_file_description_intent_into_lc_desc_upgrade.py`
- `pigeon_compiler/rename_engine/planner_seq002_v004_d0315__generate_rename_plan_for_non_lc_verify_pigeon_plugin.py`
- `pigeon_compiler/rename_engine/run_heal_seq010_v004_d0315__automated_self_healing_pipeline_lc_verify_pigeon_plugin.py`
- `src/confidence_scorer_seq033_v001.py`
- `src/research_lab_seq029_v005_d0401__the_system_studying_the_system_lc_glyph_compiler_symbol.py`
- `src/symbol_dictionary_seq031_v002_d0401__symbol_dictionary_generator_for_pigeon_lc_glyph_compiler_symbol.py`

---

**nametag** (seq011 v004): I was touched to embed a confidence glyph into file descriptions, so the planner can visually prioritize high-certainty renames. I assume the glyph format (like `[▮▮▮▯▯]`) is a fixed-width, five-character string that won’t interfere with existing description parsing. If my output changes to a different length or includes newline characters, downstream parsers in **planner** and **run_heal** will misalign columns and break plan generation. Watch for silent truncation of descriptions after the glyph.

**planner** (seq002 v005): I now consume confidence glyphs from **nametag** and symbol confidence scores from **confidence_scorer** to generate risk-weighted rename plans. I assume the glyph is always present and that **symbol_dictionary** provides a stable mapping from symbol to module. If **nametag** stops emitting the glyph or **symbol_dictionary** returns `None` for a known symbol, I’ll produce empty or malformed plans, causing **run_heal** to skip renames.

**confidence_scorer** (seq033 v002): I was extended to emit per-module confidence scores as both numeric values and the glyph bar. I assume my scoring heuristics (based on token patterns) remain stable across file versions. If my internal thresholds drift, the glyphs shown by **nametag** will misrepresent actual confidence, leading **planner** to either over-aggressively rename low-confidence items or skip high-confidence ones. A regression would be all glyphs showing full bars despite low scores.

**research_lab** (seq029 v006): I was updated to log glyph-compiler interactions, treating the confidence glyph as a new symbolic token. I assume the glyph is a single, atomic token in my logs. If the glyph format changes mid-session, my tokenization will split it, breaking pattern analysis. Watch for fractured log entries that disrupt the study of rename confidence trends.

**symbol_dictionary** (seq031 v003): I now receive confidence scores from **confidence_scorer** to weight symbol entries. I assume scores are floats between 0 and 1. If **confidence_scorer** returns out-of-range values, my weighting calculation may produce NaN or extreme values, causing **planner** to crash when sorting symbols. A regression would be missing symbols in the dictionary due to invalid score propagation.

**git_plugin**: I was added to handle versioning of glyph-related metadata. I assume the git repository is always initialized and that my hooks are called synchronously. If my commit hooks run async or fail silently, glyph metadata may not be persisted, causing **run_heal** to operate on stale confidence data.

REGRESSION WATCHLIST: glyph format changes breaking nametag/planner contract; confidence scorer threshold drift misrepresenting scores; symbol dictionary receiving out-of-range scores. This push integrates a visual confidence glyph into the rename pipeline to prioritize high-certainty changes.
