# Push Narrative — 51c097d

**Intent**: glyph_compiler_symbol  
**Date**: 2026-04-01 05:10 UTC  
**Files**: 12  
**Deleted words**: yes bu, age - like we have file append and if test run, what about glypg as signals for breakbruh  

- `src/copilot_prompt_manager_seq020/copilot_prompt_manager_seq020_orchestrator_seq010_v003_d0401__auto_extracted_by_pigeon_compiler_lc_glyph_compiler_symbol.py`
- `src/glyph_compiler_seq032_v002_d0401__glyph_compiler_python_maximum_symbolic_lc_glyph_compiler_symbol.py`
- `src/glyph_compiler_seq032_v003_d0401__glyph_compiler_v2_ast_based_lc_glyph_compiler_symbol.py`
- `src/research_lab_seq029_v005_d0401__the_system_studying_the_system_lc_glyph_compiler_symbol.py`
- `src/symbol_dictionary_seq031_v002_d0401__symbol_dictionary_generator_for_pigeon_lc_glyph_compiler_symbol.py`
- `client/chat_response_reader.py`
- `src/copilot_prompt_manager_seq020/copilot_prompt_manager_seq020_orchestrator_seq010_v002_d0322__auto_extracted_by_pigeon_compiler_lc_stage_78_hook.py`
- `src/glyph_compiler_seq032_v001.py`
- `src/glyph_compiler_seq032_v002.py`
- `src/research_lab_seq029_v004_d0331__the_system_studying_the_system_lc_mutation_patch_pipeline.py`
- `src/symbol_dictionary_seq031_v001.py`
- `vscode-extension/classify_bridge.py`

---

**copilot_prompt_manager_seq020_orchestrator** was updated to integrate the new glyph compiler as a symbolic processing stage, assuming the compiler outputs a stable, parseable symbol dictionary. If the compiler's output format changes from a serializable dict to a raw object, the orchestration pipeline will fail to marshal the data, causing a silent drop of the symbol layer in all subsequent prompts.

**glyph_compiler (v002 to v003)** was rewritten from a "maximum symbolic" approach to an AST-based architecture to improve accuracy, receiving raw code and emitting structured symbol metadata. It assumes Python's `ast` module can parse all target code snippets; malformed code or syntax beyond the targeted Python version will cause the compiler to throw an uncaught `SyntaxError` and crash the entire pipeline. It gives a symbol dictionary to the orchestrator and the research lab.

**research_lab** now consumes the glyph compiler's symbol dictionary to study system patterns, assuming the dictionary contains a 'node_type_histogram' key. If this key is missing or renamed, the lab's analysis functions will raise a KeyError, halting the research cycle and leaving mutation patches ungenerated.

**symbol_dictionary** was generated as a foundational artifact for the pigeon system, assuming a specific JSON schema that the glyph compiler must adhere to. If the compiler's output drifts from this schema—for example, nesting symbols differently—the dictionary will be invalid for downstream classifiers, causing misclassifications throughout the VSCode extension.

**chat_response_reader** and **classify_bridge** were touched to wire in the new symbolic data flow, assuming the glyph compiler's output is available in the shared client state. If the compiler fails asynchronously, these modules will receive `None` or an empty dict, leading to null-pointer exceptions when trying to access symbol attributes.

The operator's high deletion ratio and abandoned phrases ("what about glyph as signals for breakage") show hesitation about using glyph outputs as reliability metrics; they ultimately shelved that validation layer, making this push more fragile.

REGRESSION WATCHLIST: glyph compiler AST parsing on edge-case syntax, orchestrator expecting stable symbol dictionary schema, research lab depending on 'node_type_histogram' key. This push replaces a heuristic glyph compiler with an AST-driven one to produce precise symbol dictionaries for system analysis.
