# Bug Profiles — The Rogues Gallery

*Auto-generated 2026-06-06 00:01 UTC · 82 modules carrying bugs · 3 species identified*

> Every bug here is alive. They have names, habits, and a body count.
> This page tells you who they are, what they're doing to your codebase, and what to do about it.

## The Lineup

**Dead Export Shade** (`de`) — 11 modules, 16 total sightings. *Leaves dead functions standing so everyone thinks they matter.*

**Coupling Leech** (`hc`) — 40 modules, 50 total sightings. *Braids modules together until one cut hurts five files.*

**Overcap Maw** (`oc`) — 44 modules, 57 total sightings. *Swells files past the hard cap. Split before it eats context.*

## Filename β Check

The β suffix in a filename is the bug's brand. If it's missing, pigeon lost track.

- `codex_compat` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_compat_append_jsonl` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_compat_build_dynamic_context_pack` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_compat_ensure_repo_on_path` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_compat_load_json` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_compat_load_jsonl_tail` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_compat_parse_deleted_words` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_compat_refresh_state` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_compat_render_dynamic_context_pack` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_compat_repo_root` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_compat_select_context` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_compat_utc_now` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `包p_cpk` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `图p_ge` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `描p_ghm` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `环检p_ld` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `缩p_fdt` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `nl_parsers` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `pq_search_utils` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `compile_lineage` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `切p_ss` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `git_plugin` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `p_gppc` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `w_gpmo` — should be βhcoc, filename says βoc. Pigeon needs to re-stamp this one.
- `organization_pass` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `pigeon_limits` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `正f_cmp_co` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `p_ex` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `引w_ir` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `扫p_sc` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `鸽环f_rpl` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `analyze_prompt_behavior` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `agent_work_closeout` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `batch_rewrite_sim` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `cannon_execution_gate` — should be βde, filename says β(nothing). Pigeon needs to re-stamp this one.
- `codex_edit_outcome_binder` — should be βhcoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `file_email_plugin` — should be βdehcoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `file_email_text_chain` — should be βde, filename says β(nothing). Pigeon needs to re-stamp this one.
- `file_intent_identity` — should be βhcoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `file_interlinked_naming_sim` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `file_interview_mode` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `file_number_key_identity` — should be βde, filename says β(nothing). Pigeon needs to re-stamp this one.
- `file_self_sim_learning` — should be βdeoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `file_sim_deepseek_lane` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `folder_context_coupling` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `hush_intent_runtime` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `intent_attention_grader` — should be βhcoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `intent_identity_naming` — should be βhcoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `intent_loop_closer` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `intent_outcome_binder` — should be βdeoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `manifest_state_cycle` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `operator_response_policy` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `opus_micro_pulse_runtime` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `opus_orchestrator_runtime` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `opus_prompt_box` — should be βhcoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `opus_task_drift` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `pigeon_legacy_loader` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `registry_identity_bridge` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `rework_scorecard` — should be βde, filename says β(nothing). Pigeon needs to re-stamp this one.
- `root_sim_key_file` — should be βhcoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `self_fix_verification` — should be βde, filename says β(nothing). Pigeon needs to re-stamp this one.
- `tc_constants` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `tc_context` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `tc_file_encoder` — should be βdeoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `tc_intent_keys` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `tc_intent_manager` — should be βde, filename says β(nothing). Pigeon needs to re-stamp this one.
- `tc_onboard` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `tc_prompt_brain` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `tc_prompt_composer` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `tc_semantic_profile` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `thought_completer` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `p_u_pj` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `p_u_pj` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `p_upsvdλβtl_s012_v001` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `unified_manifest_state` — should be βhcoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `weakness_surfacer` — should be βde, filename says β(nothing). Pigeon needs to re-stamp this one.
- `片w_sm` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `_resolve` — should be βhc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `test_tc_intent_keys` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.
- `test_intent_attention_grader` — should be βoc, filename says β(nothing). Pigeon needs to re-stamp this one.

2/82 branded correctly. 80 missing — next rename cycle should catch them.

---
## Dead Export Shade

*Leaves dead functions standing so everyone thinks they matter.* — 11 known hosts.

### file_email_plugin

*Demon name: Export Shade of fileemai*

`file_email_plugin` has 1 dead export(s) still standing at attention like they matter. Nobody imports them. Nobody calls them. They just… sit there, consuming mental space. Remove them or give them a job. Right now they're decoration.

Spotted 3x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### file_self_sim_learning

*Demon name: Dead Echo of fileself*

`file_self_sim_learning` has 1 dead export(s) still standing at attention like they matter. Nobody imports them. Nobody calls them. They just… sit there, consuming mental space. Remove them or give them a job. Right now they're decoration.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### intent_outcome_binder

*Demon name: Export Shade of intentou*

`intent_outcome_binder` has 1 dead export(s) still standing at attention like they matter. Nobody imports them. Nobody calls them. They just… sit there, consuming mental space. Remove them or give them a job. Right now they're decoration.

Spotted 2x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### tc_file_encoder

*Demon name: Dead Echo of tcfileen*

`tc_file_encoder` has 1 dead export(s) still standing at attention like they matter. Nobody imports them. Nobody calls them. They just… sit there, consuming mental space. Remove them or give them a job. Right now they're decoration.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### cannon_execution_gate

*Demon name: Null Moth of cannonex*

`cannon_execution_gate` has 1 dead export(s) still standing at attention like they matter. Nobody imports them. Nobody calls them. They just… sit there, consuming mental space. Remove them or give them a job. Right now they're decoration.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### file_email_text_chain

*Demon name: Export Shade of fileemai*

`file_email_text_chain` has 1 dead export(s) still standing at attention like they matter. Nobody imports them. Nobody calls them. They just… sit there, consuming mental space. Remove them or give them a job. Right now they're decoration.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### file_number_key_identity

*Demon name: Dead Echo of filenumb*

`file_number_key_identity` has 1 dead export(s) still standing at attention like they matter. Nobody imports them. Nobody calls them. They just… sit there, consuming mental space. Remove them or give them a job. Right now they're decoration.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### rework_scorecard

*Demon name: Null Moth of reworksc*

`rework_scorecard` has 1 dead export(s) still standing at attention like they matter. Nobody imports them. Nobody calls them. They just… sit there, consuming mental space. Remove them or give them a job. Right now they're decoration.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### self_fix_verification

*Demon name: Dead Echo of selffixv*

`self_fix_verification` has 1 dead export(s) still standing at attention like they matter. Nobody imports them. Nobody calls them. They just… sit there, consuming mental space. Remove them or give them a job. Right now they're decoration.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### tc_intent_manager

*Demon name: Dead Echo of tcintent*

`tc_intent_manager` has 1 dead export(s) still standing at attention like they matter. Nobody imports them. Nobody calls them. They just… sit there, consuming mental space. Remove them or give them a job. Right now they're decoration.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### weakness_surfacer

*Demon name: Null Moth of weakness*

`weakness_surfacer` has 1 dead export(s) still standing at attention like they matter. Nobody imports them. Nobody calls them. They just… sit there, consuming mental space. Remove them or give them a job. Right now they're decoration.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.


---
## Coupling Leech

*Braids modules together until one cut hurts five files.* — 40 known hosts.

### file_email_plugin

*Demon name: Tangle Fiend of fileemai*

`file_email_plugin` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 3x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### w_gpmo

*Demon name: Knot Familiar of wgpmo*

`w_gpmo` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 2x across 12 versions. Last touched: tl. β in filename: **MISSING**.

### codex_edit_outcome_binder

*Demon name: Tangle Fiend of codexedi*

`codex_edit_outcome_binder` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 2x across 1 versions. Last touched: stamp_intent_touch. β in filename: **MISSING**.

### file_intent_identity

*Demon name: Tangle Fiend of fileinte*

`file_intent_identity` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### intent_attention_grader

*Demon name: Tangle Fiend of intentat*

`intent_attention_grader` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 2x across 1 versions. Last touched: wire_itid_naming. β in filename: **MISSING**.

### intent_identity_naming

*Demon name: Knot Familiar of intentid*

`intent_identity_naming` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 2x across 2 versions. Last touched: replace_seq_with_itid. β in filename: **MISSING**.

### opus_prompt_box

*Demon name: Knot Familiar of opusprom*

`opus_prompt_box` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### root_sim_key_file

*Demon name: Tangle Fiend of rootsimk*

`root_sim_key_file` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### unified_manifest_state

*Demon name: Tangle Fiend of unifiedm*

`unified_manifest_state` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### codex_compat_append_jsonl

*Demon name: Tangle Fiend of codexcom*

`codex_compat_append_jsonl` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### codex_compat_ensure_repo_on_path

*Demon name: Coupling Leech of codexcom*

`codex_compat_ensure_repo_on_path` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### codex_compat_load_json

*Demon name: Tangle Fiend of codexcom*

`codex_compat_load_json` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### codex_compat_load_jsonl_tail

*Demon name: Knot Familiar of codexcom*

`codex_compat_load_jsonl_tail` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### codex_compat_parse_deleted_words

*Demon name: Tangle Fiend of codexcom*

`codex_compat_parse_deleted_words` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### codex_compat_refresh_state

*Demon name: Tangle Fiend of codexcom*

`codex_compat_refresh_state` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### codex_compat_repo_root

*Demon name: Knot Familiar of codexcom*

`codex_compat_repo_root` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### codex_compat_select_context

*Demon name: Tangle Fiend of codexcom*

`codex_compat_select_context` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### codex_compat_utc_now

*Demon name: Tangle Fiend of codexcom*

`codex_compat_utc_now` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### 包p_cpk

*Demon name: Knot Familiar of pcpk*

`包p_cpk` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. β in filename: **MISSING**.

### 图p_ge

*Demon name: Knot Familiar of pge*

`图p_ge` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 3 versions. β in filename: **MISSING**.

### 描p_ghm

*Demon name: Knot Familiar of pghm*

`描p_ghm` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: p. β in filename: **MISSING**.

### 环检p_ld

*Demon name: Tangle Fiend of pld*

`环检p_ld` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: p. β in filename: **MISSING**.

### 缩p_fdt

*Demon name: Coupling Leech of pfdt*

`缩p_fdt` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: p. β in filename: **MISSING**.

### 切p_ss

*Demon name: Knot Familiar of pss*

`切p_ss` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 4 versions. β in filename: **MISSING**.

### pigeon_limits

*Demon name: Coupling Leech of pigeonli*

`pigeon_limits` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 0 versions. β in filename: **MISSING**.

### 正f_cmp_co

*Demon name: Tangle Fiend of fcmpco*

`正f_cmp_co` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### p_ex

*Demon name: Tangle Fiend of pex*

`p_ex` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 4 versions. β in filename: **MISSING**.

### 引w_ir

*Demon name: Coupling Leech of wir*

`引w_ir` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 5 versions. Last touched: fx. β in filename: **MISSING**.

### 扫p_sc

*Demon name: Tangle Fiend of psc*

`扫p_sc` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 4 versions. β in filename: **MISSING**.

### intent_loop_closer

*Demon name: Coupling Leech of intentlo*

`intent_loop_closer` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### pigeon_legacy_loader

*Demon name: Knot Familiar of pigeonle*

`pigeon_legacy_loader` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### tc_constants

*Demon name: Coupling Leech of tcconsta*

`tc_constants` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### tc_context

*Demon name: Coupling Leech of tccontex*

`tc_context` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### tc_intent_keys

*Demon name: Knot Familiar of tcintent*

`tc_intent_keys` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### tc_prompt_brain

*Demon name: Knot Familiar of tcprompt*

`tc_prompt_brain` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### p_u_pj

*Demon name: Knot Familiar of pupj*

`p_u_pj` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### p_u_pj

*Demon name: Knot Familiar of pupj*

`p_u_pj` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### p_upsvdλβtl_s012_v001

*Demon name: Coupling Leech of pupsvdtl*

`p_upsvdλβtl_s012_v001` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 0 versions. β in filename: **MISSING**.

### 片w_sm

*Demon name: Tangle Fiend of wsm*

`片w_sm` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 2 versions. Last touched: f. β in filename: **MISSING**.

### _resolve

*Demon name: Tangle Fiend of resolve*

`_resolve` braided itself to (unknown) so tightly that touching one means touching all 0. Extract the shared logic into a common module. Or accept the pain every time you edit.

Spotted 1x across 0 versions. β in filename: **MISSING**.


---
## Overcap Maw

*Swells files past the hard cap. Split before it eats context.* — 44 known hosts.

### file_email_plugin

*Demon name: Overcap Maw of fileemai*

`file_email_plugin` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 3x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### w_gpmo

*Demon name: Split Fiend of wgpmo*

`w_gpmo` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v12, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 2x across 12 versions. Last touched: tl. β in filename: **MISSING**.

### codex_edit_outcome_binder

*Demon name: Overcap Maw of codexedi*

`codex_edit_outcome_binder` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 2x across 1 versions. Last touched: stamp_intent_touch. β in filename: **MISSING**.

### file_intent_identity

*Demon name: Overcap Maw of fileinte*

`file_intent_identity` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### file_self_sim_learning

*Demon name: Shard Hunger of fileself*

`file_self_sim_learning` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### intent_attention_grader

*Demon name: Overcap Maw of intentat*

`intent_attention_grader` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 2x across 1 versions. Last touched: wire_itid_naming. β in filename: **MISSING**.

### intent_identity_naming

*Demon name: Split Fiend of intentid*

`intent_identity_naming` came in wheezing at 2161 tokens — that's 8% over the 2000-token hard cap. Every push it gains weight. v2, still unsplit. Over the line but not emergency. Schedule a split this push cycle. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 2x across 2 versions. Last touched: replace_seq_with_itid. β in filename: **MISSING**.

### intent_outcome_binder

*Demon name: Overcap Maw of intentou*

`intent_outcome_binder` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v2, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 2x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### opus_prompt_box

*Demon name: Split Fiend of opusprom*

`opus_prompt_box` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### root_sim_key_file

*Demon name: Overcap Maw of rootsimk*

`root_sim_key_file` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### tc_file_encoder

*Demon name: Shard Hunger of tcfileen*

`tc_file_encoder` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### unified_manifest_state

*Demon name: Overcap Maw of unifiedm*

`unified_manifest_state` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 2x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### codex_compat

*Demon name: Split Fiend of codexcom*

`codex_compat` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v0, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 0 versions. β in filename: **MISSING**.

### codex_compat_build_dynamic_context_pack

*Demon name: Shard Hunger of codexcom*

`codex_compat_build_dynamic_context_pack` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v2, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### codex_compat_render_dynamic_context_pack

*Demon name: Split Fiend of codexcom*

`codex_compat_render_dynamic_context_pack` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v2, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### nl_parsers

*Demon name: Split Fiend of nlparser*

`nl_parsers` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v3, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 3 versions. Last touched: desc_upgrade. β in filename: **MISSING**.

### pq_search_utils

*Demon name: Split Fiend of pqsearch*

`pq_search_utils` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v3, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 3 versions. Last touched: desc_upgrade. β in filename: **MISSING**.

### compile_lineage

*Demon name: Overcap Maw of compilel*

`compile_lineage` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v0, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 0 versions. β in filename: **MISSING**.

### git_plugin

*Demon name: Shard Hunger of gitplugi*

`git_plugin` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v0, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 0 versions. β in filename: **MISSING**.

### p_gppc

*Demon name: Shard Hunger of pgppc*

`p_gppc` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### organization_pass

*Demon name: Overcap Maw of organiza*

`organization_pass` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### 净拆f_rcs

*Demon name: Overcap Maw of frcs*

`净拆f_rcs` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v7, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 7 versions. Last touched: tl. β in filename: yes.

### 鸽环f_rpl

*Demon name: Overcap Maw of frpl*

`鸽环f_rpl` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v4, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 4 versions. β in filename: **MISSING**.

### analyze_prompt_behavior

*Demon name: Overcap Maw of analyzep*

`analyze_prompt_behavior` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v0, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 0 versions. β in filename: **MISSING**.

### agent_work_closeout

*Demon name: Shard Hunger of agentwor*

`agent_work_closeout` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### batch_rewrite_sim

*Demon name: Shard Hunger of batchrew*

`batch_rewrite_sim` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v2, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### file_interlinked_naming_sim

*Demon name: Shard Hunger of fileinte*

`file_interlinked_naming_sim` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### file_interview_mode

*Demon name: Shard Hunger of fileinte*

`file_interview_mode` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### file_sim_deepseek_lane

*Demon name: Split Fiend of filesimd*

`file_sim_deepseek_lane` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### folder_context_coupling

*Demon name: Split Fiend of folderco*

`folder_context_coupling` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### hush_intent_runtime

*Demon name: Overcap Maw of hushinte*

`hush_intent_runtime` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### manifest_state_cycle

*Demon name: Shard Hunger of manifest*

`manifest_state_cycle` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### operator_response_policy

*Demon name: Shard Hunger of operator*

`operator_response_policy` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v2, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 2 versions. Last touched: feat_bind_keystroke_telemetry. β in filename: **MISSING**.

### opus_micro_pulse_runtime

*Demon name: Overcap Maw of opusmicr*

`opus_micro_pulse_runtime` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### opus_orchestrator_runtime

*Demon name: Overcap Maw of opusorch*

`opus_orchestrator_runtime` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### opus_task_drift

*Demon name: Shard Hunger of opustask*

`opus_task_drift` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### registry_identity_bridge

*Demon name: Shard Hunger of registry*

`registry_identity_bridge` came in wheezing at 3485 tokens — that's 74% over the 2000-token hard cap. Every push it gains weight. v2, still unsplit. Significant bloat. Every prompt that touches this file pays the tax. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 2 versions. Last touched: patch_registry. β in filename: **MISSING**.

### tc_onboard

*Demon name: Split Fiend of tconboar*

`tc_onboard` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### tc_prompt_composer

*Demon name: Split Fiend of tcprompt*

`tc_prompt_composer` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### tc_semantic_profile

*Demon name: Shard Hunger of tcsemant*

`tc_semantic_profile` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v1, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 1 versions. Last touched: registered. β in filename: **MISSING**.

### thought_completer

*Demon name: Split Fiend of thoughtc*

`thought_completer` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v0, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 0 versions. β in filename: **MISSING**.

### 修f_sf

*Demon name: Overcap Maw of fsf*

`修f_sf` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v12, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 12 versions. Last touched: vr. β in filename: yes.

### test_tc_intent_keys

*Demon name: Split Fiend of testtcin*

`test_tc_intent_keys` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v0, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 0 versions. β in filename: **MISSING**.

### test_intent_attention_grader

*Demon name: Overcap Maw of testinte*

`test_intent_attention_grader` came in wheezing at 0 tokens — that's -100% over the 2000-token hard cap. Every push it gains weight. v0, still unsplit. Just barely over. Keep an eye on it. The pigeon compiler can carve this into shards in one command. The question is: when.

Spotted 1x across 0 versions. β in filename: **MISSING**.

